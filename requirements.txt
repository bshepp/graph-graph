# graph-graph dependencies

# Core
numpy>=1.20
networkx>=2.6
scipy>=1.7
matplotlib>=3.5
tqdm>=4.60

# Optional: GPU acceleration for large scale
# torch>=2.0
# torch-geometric>=2.0
# cudf  # RAPIDS for GPU graphs

# Optional: quantum hardware experiments only.
# braket_walks.py runs on the core scipy stack above without this; the SDK is needed
# solely for the deferred circuit-based (DTQW) hardware path documented in that file.
# amazon-braket-sdk>=1.50

